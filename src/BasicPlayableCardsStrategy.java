import java.util.ArrayList;
import java.util.List;

public class BasicPlayableCardsStrategy implements PlayableCardsStrategy{
    @Override
    public List<Card> getPlayableCards(Player player, Card card) {
        List<Card> playableCards = new ArrayList<>();

        return playableCards;
    }
}
